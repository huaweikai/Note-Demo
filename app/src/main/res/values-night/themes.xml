<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Note" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="ThemeOverlay.search" parent="Theme.Note">
        <item name="android:editTextColor">@color/white</item>
        <item name="android:textColorHint">@color/gray</item>
        <item name="android:background">@drawable/background_black</item>
        <item name="android:searchIcon">@drawable/ic_baseline_search_black</item>
    </style>
    <style name="ThemeOverlay.Image" parent="Theme.Note">
        <item name="android:background"></item>
        <item name="android:tint">@color/white</item>
    </style>
    <style name="ThemeOverlay.Down" parent="Theme.Note">
        <item name="android:background">@drawable/background_black</item>
    </style>
    <style name="ThemeOverlayColor.All" parent="Theme.Note">
        <item name="android:background">@color/black</item>
    </style>



    <style name="ThemeOverlay.more" parent="Theme.Note">
        <item name="android:tint">@color/white</item>
    </style>


    <style name="ThemeOverlay.Button" parent="Theme.Note">
        <item name="android:backgroundTint">@color/accent</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>